#pragma once

// Copy this file to wifi_secrets.h (which is gitignored) and fill in your
// network. With no wifi_secrets.h, or an empty SSID, the firmware skips
// Wi-Fi entirely and the PCF85063 RTC keeps time on its own.

#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-password"
